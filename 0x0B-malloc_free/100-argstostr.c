#include "main.h"

/**
 * argstostr - args
 * @ac: ac
 * @len: len
 * @str: str
 * @**av: av
 * Reurn: null if ac is 0
 */

char *argstostr(int ac, char **av)
{
	int len = 0

		if (str != NULL)
		{
			while (str[len])
				len++;
		}
	return(len);
}
